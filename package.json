{
  "name": "@workspace/root",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run -w @workspace/api dev\" \"npm run -w @workspace/web dev\"",
    "build": "npm run -ws build",
    "start": "concurrently -n api,web -c cyan,magenta \"npm run -w @workspace/api start\" \"npm run -w @workspace/web preview\"",
    "lint": "npm -ws run lint --if-present",
    "test": "npm -ws test --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
